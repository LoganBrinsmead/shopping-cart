/**
 * the shopping page for the site
 * can filter cars with sidebar that displays makes
 * use useEffect for above implementation
 */
import React from "react";

export default function ShoppingPage(props) {
    /**
     * pass car make as array in props,
     * map over cars array and render the separate car pages
     * 
     */
}